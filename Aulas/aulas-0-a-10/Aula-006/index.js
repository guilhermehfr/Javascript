const nome = 'joão';        // String
const sobrenome = 'pedro';  // String
const idade = 30;           // Number
const altura = 1.82;        // Number

let peso;                   // Valor = Undefined -> Indefinido, não aponta para nenhum lugar na memória.
const corDoFundo = null;    // Valor = Null      -> Nulo,       não aponta para nenhum lugar na memória.

const aprovado = false;     // Boolean = True ou False (Fatores lógicos)

console.log(`${nome} ${sobrenome} tem ${idade} anos e ${altura} metros de altura.`);
console.log(peso, typeof(peso));
console.log(typeof(nome), nome);